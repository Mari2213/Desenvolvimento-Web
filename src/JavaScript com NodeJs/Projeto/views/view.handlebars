<div id="job-view-container" class="container">
  <div class="row text-center mt-lg-5">
    <a class="back-link main-color" href="/">Voltar</a>
    <div id="img-container" class="col-md-12">
      <img src="/img/company.jpg" alt="Company" class="w-50" />
    </div>
    <div class="col-md-12">
      <h1 class="main-color">{{job.dataValues.title}}</h1>
      <p class="fw-bold">Quem eles estão buscando:</p>
      <p class="job-desc">{{job.dataValues.description}}</p>
      <p><span class="fw-bold">O salário informado é de:</span>
        R${{job.dataValues.salary}}</p>
      <p>
        Para trabalhar na
        <span class="fw-bold main-color">{{job.dataValues.company}}</span>
        envie um email para a
        <a href="mail:to{{job.dataValues.email}}">{{job.dataValues.email}}</a>
      </p>
    </div>
  </div>
</div>